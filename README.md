# Agent-2-Email Marketing Engine
Owns database segmentation and all 10 email sequences. Listens to Customer Relationship Management (CRM) webhooks, writes personalised emails using lender/rate/suburb data, sends via Application Programming Interface (API), tracks opens/clicks/replies, runs Reserve Bank of Australia (RBA) trigger campaigns, and sends weekly digests.
